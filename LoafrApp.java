public class LoafrApp {
    public static void main(String[] args) {
        // Initialize components
        Controller controller = new Controller();
        // Start the system
        controller.run();
    }
}
